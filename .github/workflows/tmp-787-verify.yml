# TEMPORARY #787 verification — do not keep on master.
name: Temporary #787 omzet filter verification

on:
  pull_request:
  push:

jobs:
  verify-omzet-filter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          coverage: none

      - name: Run temporary PRJ2602236 omzet filter test
        run: php tests/tmp_787_prj2602236_omzet_filter.php
